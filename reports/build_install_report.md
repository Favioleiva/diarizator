# Build and clean-install report

Wheel and source distribution built successfully as `diarizator-0.1.0-py3-none-any.whl` and `diarizator-0.1.0.tar.gz`. Each was installed with `--no-deps` into a separate clean Python 3.12 virtual environment. Both passed `import diarizator`, version 0.1.0, and CLI help. The wheel also passed `environment` and controlled-demo `inspect`; the installed demo reported the expected hash and 370.050612-second duration.
