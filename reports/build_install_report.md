# Build and clean-install report

After correcting the public notebook and generator, wheel and source distribution built successfully as `diarizator-0.1.0-py3-none-any.whl` and `diarizator-0.1.0.tar.gz`. Each installed with `--no-deps` into a separate clean Python 3.12 virtual environment and passed `import diarizator`, version 0.1.0, and CLI help. Distribution inspection compiled the corrected notebook inside the sdist, confirmed that the wheel intentionally contains no notebook, enforced the approved-demo audio allowlist, and found no prohibited paths, secrets, placeholders, or private paths.
