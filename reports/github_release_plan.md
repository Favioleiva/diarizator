# GitHub release plan

Publish the initial commit to `main`, let all three GitHub Actions workflows pass, open the notebook from the GitHub Colab badge in a fresh GPU runtime, accept Community-1 access, and complete an authenticated upload/download smoke run. Review the demo transcript and reconfirm its audio license declaration. Only then create `v0.1.0`, pin the stable notebook to that tag, rebuild, and prepare a GitHub Release. Do not tag before these checks.
