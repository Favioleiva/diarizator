# Test report

`python -m pytest -q`: **38 passed**. Coverage includes imports, API modes, CLI smoke, MP3/WAV metadata, supported formats, Unicode and spaced filenames, access status, secret/path redaction, normal/exclusive timelines, attribution/Unknown behavior, configuration validation, output validation, ZIP exclusion, notebook structure, legal metadata, and the audio allowlist. Gated real-model execution is intentionally excluded from public CPU CI.
